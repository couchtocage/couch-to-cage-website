const clean=v=>v==null?null:String(v).trim()||null;
async function insert(table,row){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error('Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY');const res=await fetch(`${url}/rest/v1/${table}`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(row)});if(!res.ok)throw new Error(`${table} sync failed: ${res.status} ${await res.text()}`)}
export default {async formSubmitted(event){const d=event.data||{},name=d['form-name'];if(name==='ctc-team-application'){await insert('team_applications',{full_name:clean(d.full_name),email:clean(d.email),phone:clean(d.phone),city:clean(d.city),state:clean(d.state),position:clean(d.position),experience:clean(d.experience),portfolio:clean(d.portfolio),instagram:clean(d.instagram),youtube:clean(d.youtube),resume_url:clean(d.resume),work_samples_url:clean(d.work_samples),about_yourself:clean(d.about_yourself),why_join_ctc:clean(d.why_join_ctc),volunteer_confirmation:clean(d.volunteer_confirmation),submission_data:d})}else if(name==='ctc-sponsor-inquiry'){await insert('sponsor_inquiries',{contact_name:clean(d.contact_name),company_name:clean(d.company_name),email:clean(d.email),phone:clean(d.phone),website:clean(d.website),social_handle:clean(d.social_handle),sponsorship_type:clean(d.sponsorship_type),budget:clean(d.budget),about_brand:clean(d.about_brand),what_to_sponsor:clean(d.what_to_sponsor),partner_goals:clean(d.partner_goals),brand_materials_url:clean(d.brand_materials),contact_agreement:clean(d.contact_agreement),submission_data:d})}}};
